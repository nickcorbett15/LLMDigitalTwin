version: '3'
services:
  react-app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules  # prevent host's node_modules from interfering
    environment:
      - CHOKIDAR_USEPOLLING=true
